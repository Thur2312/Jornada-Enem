services:

  db:
    image: postgres
    restart: always
 
    shm_size: 128mb
    
    environment:
      POSTGRES_USER: johndoe
      POSTGRES_PASSWORD: 1234
      POSTGRES_DB: mydb

  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080

      
